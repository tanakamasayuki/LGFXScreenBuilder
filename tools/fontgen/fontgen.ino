// Font-catalog host harness (SPEC §8.7.2). Phase 1a uses this sketch only as the
// pinned version source for tools/gen-fonts.mjs (building it downloads the exact
// LovyanGFX / M5GFX copies into ~/.arduino15/internal). Phase 1b will fill setup()
// to introspect each preset font (metrics / coverage / data size) and render a
// sample PNG per font for the adoption preview.
#include <LovyanGFX.hpp>
#include <LGFX_AUTODETECT.hpp>

void setup() {}
void loop() {}
